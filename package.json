{
  "name": "portfolio-dashboard",
  "version": "0.1.0",
  "description": "A tool to use as a stock trading dashboard similar to Wallmine's portfolio but with additional features.",
  "main": "index.js",
  "scripts": {
    "migrate:dev": "cd src && sequelize --config config/sequelize.config.js db:migrate",
    "test": "jest --watch --colors tests/*",
    "dev": "nodemon src"
  },
  "keywords": [],
  "author": "Kerim Güney <kerim@gueney.io> (https://gueney.io)",
  "license": "MIT",
  "devDependencies": {
    "eslint": "^7.5.0",
    "jest": "^26.2.1",
    "nodemon": "^2.0.4",
    "prettier": "^2.0.5"
  },
  "dependencies": {
    "csv-parse": "^4.11.1",
    "sequelize": "^6.29.0",
    "sqlite3": "^5.0.3"
  }
}
